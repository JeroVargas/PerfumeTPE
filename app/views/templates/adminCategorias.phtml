<?php require 'app/views/templates/layout/header.phtml' ?>

<main class="container py-5">
    <section class="product-section">
        <h2 class="mb-3">Panel de Administración: Categorías</h2>
        <p class="section-description">Agregar, editar o eliminar las familias olfativas del sistema.</p>

        <div class="mb-5">
            <a href="form-add-categoria" class="btn" style="display: inline-block; text-decoration: none; font-size: 0.9rem; padding: 10px 24px;">
                + Nueva Categoría
            </a>
        </div>

        <?php if (empty($categorias)): ?>
            <div class="alert alert-info text-center" style="background: #18181a; color: #888; border: 1px solid rgba(255,255,255,0.05); border-radius: 14px;">
                No hay categorías para administrar.
            </div>
        <?php else: ?>
            <div class="card-grid">
                <?php foreach ($categorias as $cat): ?>
                    <div class="item-card d-flex flex-column justify-content-between">
                        <div class="item-card-content">
                            <?php if (!empty($cat->imagen)): ?>
                                <img src="<?= htmlspecialchars($cat->imagen) ?>" alt="<?= htmlspecialchars($cat->nombre) ?>" 
                                     style="width: 100%; height: 180px; object-fit: cover; border-radius: 8px; margin-bottom: 15px; border: 1px solid rgba(255,255,255,0.05);">
                            <?php endif; ?>

                            <h3><?= htmlspecialchars($cat->nombre) ?></h3>
                        </div>

                        <div class="item-card-footer" style="margin-top: 20px;">
                            <a href="detalle_categoria/<?= $cat->id ?>" style="color: #d4a373; text-decoration: none; font-weight: 600; font-size: 0.95rem;">Ver vista previa →</a>
                            
                            <div class="admin-actions" style="display: flex; gap: 10px; border-top: 1px solid rgba(255,255,255,0.05); padding-top: 15px; margin-top: 15px;">
                                <a href="show-edit-categoria/<?= $cat->id ?>" class="btn-editar" style="text-decoration: none; font-size: 0.8rem; padding: 6px 14px; display: inline-block;">Editar</a>
                                <a href="delete-categoria/<?= $cat->id ?>" class="btn-eliminar" style="text-decoration: none; font-size: 0.8rem; padding: 6px 14px; display: inline-block;" 
                                   onclick="return confirm('¿Estás seguro de que deseas eliminar la categoría <?= $cat->nombre ?>?')">Eliminar</a>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>
    </section>
</main>

<?php require 'app/views/templates/layout/footer.phtml' ?>