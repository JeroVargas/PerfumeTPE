<?php require 'app/views/templates/layout/header.phtml' ?>

<main class="container py-5">
    <section class="product-section">

        <h2 class="mb-3">
            <?= !empty($categoria) ? 'Perfumes de ' . htmlspecialchars($categoria[0]->nombre_categoria) : 'Categoría' ?>
        </h2>

        <p class="section-description">
            Descubre los perfumes disponibles en esta categoría.
        </p>

        <?php if (empty($categoria)): ?>
            <div class="alert alert-info text-center">
                No hay perfumes en esta categoría.
            </div>
        <?php else: ?>
            <div class="card-grid">
                <?php foreach ($categoria as $perfume): ?>
                    <div class="item-card">
                        <div class="item-card-content">
                            <h3><?= htmlspecialchars($perfume->nombre) ?></h3>
                            <p><strong>Precio:</strong> $<?= htmlspecialchars($perfume->precio) ?></p>
                        </div>

                        <div class="item-card-footer">
                            <a href="detalle_perfume/<?= $perfume->id ?>">
                                Ver detalles →
                            </a>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>

    </section>
</main>

<?php require 'app/views/templates/layout/footer.phtml' ?>