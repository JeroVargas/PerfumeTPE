<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <base href="<?= BASE_URL ?>">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Fuente y CSS -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="public/css/style.css">

    <title>Aura Premium | Alta Perfumería</title>

</head>

<body>
    <!-- HEADER / NAVBAR -->
    <nav>
        <div class="logo">
            <a href="index" style="text-decoration: none; display: flex; align-items: center; gap: 10px;">
                <svg width="34" height="34" viewBox="0 0 24 24" fill="none" stroke="#d4a373" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 22a7 7 0 0 0 7-7c0-4.3-7-13-7-13S5 10.7 5 15a7 7 0 0 0 7 7z"/>
                </svg>
                <span style="color: #fff; font-weight: 700; font-size: 1.4rem; letter-spacing: 1px; font-family: 'Poppins', sans-serif;">
                    AURA<span style="color: #d4a373;">.</span>
                </span>
            </a>
        </div>

        <div>
            <ul class="menu">
                <li><a href="index">Inicio</a></li>
                <li><a href="listaPerfumes">Productos</a></li>
                <li><a href="listaCategorias">Categorías</a></li>
                <?php
                if (session_status() == PHP_SESSION_NONE) {
                    session_start();
                }
                if (isset($_SESSION['USER_ID'])) : 
                ?>
                    <?php if ($_SESSION['USER_LEVEL'] === 'admin') : ?>
                        <li><a href="panel_de_control">Admin Perfumes</a></li>
                        <li><a href="admin-categorias">Admin Categorías</a></li>
                    <?php endif; ?>
                    <li><span class="user-email"><?= htmlspecialchars($_SESSION['USER_EMAIL']) ?></span></li>
                    <li><a href="logout">Logout</a></li>
                <?php else : ?>
                    <li><a href="login">Inicio de Sesion</a></li>
                    <li><a href="register">Registro</a></li>
                <?php endif; ?>
            </ul>
        </div>
    </nav>